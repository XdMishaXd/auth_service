env: "prod"

rabbitmq:
  queue_name: "notificationsQueue"

base_url: "https://localhost"

mailer_pool_size: 5

email:
  host: "smtp.gmail.com"
  port: 587

http_server:
  address: ":8081"
  timeout: 4s
  idle_timeout: 30s
